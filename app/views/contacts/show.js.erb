var contact = JSON.parse("<%= j @contact.to_json.html_safe %>");
var response = 'Gracias por contactarte ' + contact.name + ', reponderemos tu consulta a la brevedad.';
var form = document.getElementById('new_contact');
$(form).submit(function(){return false});
Utils.insertFormResponse(form, response);
Utils.notification('notice', 'Contacto enviado con exito.');
Utils.loader().hide();